<h1>See all the Queries here:</h1>
 
<table style="width:50%">
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>Query</th>
  </tr>
 
  <% @contacts.each do |contact| %>
    <tr>
      <td><%= contact.Name %></td>
      <td><%= contact.Email %></td>
      <td><%= contact.Query %></td>
    </tr>
  <% end %>
</table>
<br/><br/>
<%= link_to 'Create new Query', new_contact_path %>
OR
<%= link_to 'Go to Home', controller: 'articles' %>